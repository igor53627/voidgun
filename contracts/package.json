{
  "name": "voidgun-contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "forge build && FOUNDRY_PROFILE=verifier forge build verifier/TransferVerifier.sol",
    "build:verifier": "FOUNDRY_PROFILE=verifier forge build verifier/TransferVerifier.sol",
    "test": "forge test",
    "tenderly:list": "tsx scripts/tenderly.ts list",
    "tenderly:create": "tsx scripts/tenderly.ts create",
    "tenderly:reset": "tsx scripts/tenderly.ts reset",
    "deploy": "tsx scripts/Deploy.ts",
    "deploy:reset": "RESET_VNET=true tsx scripts/Deploy.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "viem": "^2.21.0"
  }
}
